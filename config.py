# Thresholds and drawing configuration for the MVP

SQUAT_DOWN_ANGLE = 95      # knee angle (deg) <= means "down"
SQUAT_UP_ANGLE   = 165     # knee angle (deg) >= means "up/standing"

PUSHUP_DOWN_ANGLE = 95     # elbow angle (deg) <= means "down"
PUSHUP_UP_ANGLE   = 165    # elbow angle (deg) >= means "up/plank"

# Minimum time (frames) to remain in down position to count as good depth (anti-bounce)
MIN_DOWN_HOLD_FRAMES = 3

# Drawing
SHOW_SKELETON_DEFAULT = True
WINDOW_NAME = "AI Form Coach (MVP)"
